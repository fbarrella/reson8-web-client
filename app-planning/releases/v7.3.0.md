# 📦 Reson8 Web Client v7.3.0 Release Notes

Third fix in the post-launch improvements round: custom emoji reactions finally look like emoji.

## 🐛 Bug Fixes

- Reactions using a custom emoji now render the approved image, not the literal `:name:` token text — matching how custom emoji already rendered correctly inside message body text.
