# 📦 Reson8 Web Client v7.3.1 Release Notes

A small icon-consistency polish item from the post-launch improvements round.

## 🐛 Bug Fixes

- The "add reaction" and "insert emoji" buttons now both use a smiley-face icon (matching the desktop client), replacing a confusing `+` icon and a literal 🙂 character that risked reading as "send this exact emoji."
