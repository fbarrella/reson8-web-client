================================================================================
  RESON8 WEB CLIENT — Development Progress Log
================================================================================

PROJECT OVERVIEW
================
Reson8 Web Client is a mobile-first, fully responsive Progressive Web App
client for Reson8 (a self-hosted voice & text communication server). Built
with React + TypeScript + Vite. It is an independent client for the existing
`reson8` server — see CLAUDE.md for the frozen wire-protocol reference and
../reson8/ for the server/desktop-client project this one targets (read-only
reference, never a dependency to build against directly).

The authoritative scope document is app-planning/00-master-prd.md and the
phase PRDs (app-planning/01 through 07-phase*.md) — read those before
assuming a feature is in/out of scope for a given phase.

Tech Stack: React + TypeScript + Vite, Zustand, Tailwind CSS + shadcn/ui
(Radix), socket.io-client, mediasoup-client, vite-plugin-pwa.

Current status: Phase 1 (foundation, connection, PWA shell) implemented —
see the entries below for what's done and what's left before the phase's
acceptance criteria are formally closed out.


================================================================================
  ONGOING LOG — new entries go below this line
================================================================================

Use `/log-progress` (see .claude/commands/log-progress.md) to append entries
here in the established format:

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.1: Project scaffold

  Problem: The repo held only the PRD set — no application code, tooling,
  or package.json existed yet.

  Solution: Scaffolded a Vite + React 19 + TypeScript project by hand
  (rather than `npm create vite` into the already-non-empty repo root):
  copied the current `react-ts` template's src/public/index.html/vite.config
  baseline, then layered on TS strict mode (all flags explicit, not just
  `strict: true`, per the PRD's wording), a `@/*` -> `src/*` path alias,
  ESLint flat config (`@typescript-eslint` via `typescript-eslint`,
  `eslint-plugin-react-hooks`, `eslint-plugin-jsx-a11y`, `eslint-plugin-
  react-refresh`, `eslint-config-prettier`), Prettier, Vitest + React
  Testing Library + jsdom, and Playwright (chromium, mobile-chrome via
  Pixel 7 emulation, mobile-safari via iPhone 14 emulation / webkit).
  `package.json` starts at version `0.1.0` per the PRD.

  Environment note (read before running installs in this sandbox): this
  proot/ARM64 environment has two networking quirks that make plain `npm
  install` unreliable —
    1. Node's Happy-Eyeballs dual-stack connect hangs for minutes on this
       host's broken IPv6 path. Fix: run npm/node with
       `NODE_OPTIONS=--network-family-autoselection=false` (or `export` it
       once per shell) — this took install times from 10+ minutes/package
       down to ~1-3s/package.
    2. npm's cacache tarball-rename can race under this filesystem's
       compression layer, surfacing as `ENOENT ... syscall rename` on a
       tmp->content-addressed path. It's usually transient — delete the one
       named stale cache entry (or the whole `_cacache` dir) and retry.
       Installing with a project-local `--cache` dir (not the default
       `~/.npm`) avoids a separate, harder-to-reproduce variant of the same
       symlink issue. Prefer `--maxsockets 1` for larger batches — it
       trades speed for eliminating the concurrent-write race almost
       entirely.
  Also hit two real (non-environment) issues worth remembering: (a)
  `@testing-library/dom` is a *peer* dependency of `@testing-library/react`
  v16, not a transitive one — must be installed explicitly; (b) the
  current `@vitejs/plugin-react@6.x` has an optional peer on
  `@rolldown/plugin-babel` (Babel 8) that conflicts with `vite-plugin-pwa`'s
  `workbox-build` (Babel 7) chain — resolved with `--legacy-peer-deps`,
  safe here since the Rolldown/Babel-8 path isn't used.

  Key Files Modified:
    package.json (NEW)
    tsconfig.json, tsconfig.app.json, tsconfig.node.json (NEW)
    vite.config.ts (NEW)
    eslint.config.js (NEW)
    .prettierrc.json, .prettierignore (NEW)
    playwright.config.ts (NEW)
    .gitignore (NEW entries: node_modules, dist, dev-dist, .npm-cache,
      coverage, playwright-report, test-results)
    index.html, src/main.tsx, src/vite-env.d.ts (NEW)
    src/test/setup.ts (NEW)

  Verification:
    npm run typecheck, npm run lint, npm run test, npm run build all pass.
    npm run test:e2e passes on chromium + mobile-chrome; mobile-safari
    (webkit) crashes on page.goto in this specific proot/ARM64 sandbox —
    confirmed as a sandbox/webkit-on-ARM limitation (page crash, not an
    assertion failure) via repeated runs, not a product bug. Flagging for
    Phase 7's real-device pass rather than blocking Phase 1 on it.

  Next Step: P1.2 — vendor the protocol types.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.2: Vendored protocol types

  Problem: This repo needs the `reson8` server's wire-protocol types
  without a build-time dependency on the sibling repo.

  Solution: Copied `packages/shared-types/src/{models,socket-events,
  index}.ts` from `../reson8` verbatim into `src/types/reson8-protocol/`,
  each prefixed with a header comment recording the source path, the
  desktop repo's version (1.4.1) and commit (8ccd0cc78921ef3f8042b79e
  3650496a06444767) at copy time, and copy date. No content changes beyond
  the header, per the PRD. Cross-checked the copied contract against
  CLAUDE.md's protocol mirror section — they agree; no drift to reconcile.
  Excluded this directory from ESLint (see P1.1's config) since it's a
  read-only snapshot, not code we author.

  Key Files Modified:
    src/types/reson8-protocol/models.ts (NEW)
    src/types/reson8-protocol/socket-events.ts (NEW)
    src/types/reson8-protocol/index.ts (NEW)

  Verification:
    npm run typecheck passes with the vendored types imported throughout
    the app (socketService, connectionService, channelTreeStore, etc.).

  Next Step: P1.3 — design tokens & shadcn/ui primitives.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.3: Design tokens & UI primitives

  Problem: No design system existed — colors, spacing, and the accessible
  interactive primitives (Dialog/Sheet/Tabs/Toast/etc.) the rest of the
  phase depends on hadn't been installed or themed.

  Solution: Tailwind v4 (CSS-first config, no tailwind.config.js) via
  `@tailwindcss/vite`. Single dark theme per master PRD §5.2: OKLCH color
  tokens defined once on `:root` in src/index.css and re-exposed through
  `@theme inline` so Tailwind utilities (`bg-background`, `text-primary`,
  etc.) resolve to them — structured so a future light theme is a token
  swap, not a rewrite, without actually building one this phase. Installed
  the full Radix/shadcn primitive set the PRD calls for (Dialog, Tabs,
  Toast, DropdownMenu, Popover, Slider, Avatar, Slot, Label) plus `vaul`
  for Drawer, though the Settings sheet built this phase (P1.10) still
  uses a hand-rolled dialog rather than the Radix one — see that entry.
  Hand-authored Button/Input/Label/Badge (shadcn's canonical, Radix-free
  implementations) with `cva` + `cn()` (clsx + tailwind-merge). All
  interactive sizes respect the 44x44px touch-target minimum (master PRD
  §5.2 / WCAG 2.5.5).

  Key Files Modified:
    src/index.css (NEW) — design tokens + Tailwind entry
    src/lib/utils.ts (NEW) — cn() helper
    src/components/ui/button.tsx, input.tsx, label.tsx, badge.tsx (NEW)
    eslint.config.js — per-file rule overrides for src/components/ui/**
      (react-refresh/only-export-components and jsx-a11y/label-has-
      associated-control both false-positive on shadcn's standard
      component shape)

  Verification:
    npm run lint passes (0 problems). axe-core smoke pass on the connect
    screen (Button/Input/Label usage) — see P1.6's entry for the fix that
    made it pass (a landmark-region violation, not primitive-related).

  Next Step: P1.4 — app shell & responsive composition.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.4: App shell & responsive composition

  Problem: No routing or layout shell existed to compose the mobile
  nav-stack / tablet two-pane / desktop three-pane breakpoints described
  in master PRD §5.1.

  Solution: React Router routes: `/connect`, `/app` (+ index and
  `channels/:channelId`, `chat`, `dms`, `dms/:partnerId`, `voice` children),
  guarded by `<RequireConnection>` (redirects to /connect unless
  connectionStore.status === "connected") and `<RedirectIfConnected>` (the
  inverse, so an already-connected session bounces off /connect on
  back-navigation). `<AppShell>` composes one component set three ways via
  Tailwind breakpoints: the channel tree pane is `hidden lg:block` (desktop
  three-pane); a `md:`-only overlay drawer (Radix-free — a fixed-position
  backdrop + panel toggled by uiStore) stands in for the tablet two-pane
  drawer; the bottom tab bar (Channels/Chat/DMs/Voice, `lg:hidden`) and a
  reserved-but-empty `<VoiceMiniBar>` slot (Phase 2 populates it) cover
  mobile. `ChannelsPage` renders the tree inline (`lg:hidden`) as the
  mobile/tablet main content and a "select a channel" placeholder at
  `lg:`, so the three-pane desktop view doesn't show the tree twice.

  Bug caught and fixed during manual verification: the header's hamburger
  ("Open channels") button was visible at the mobile breakpoint but its
  target drawer only renders at `md:` — a dead button on phones. Fixed by
  flipping the button's breakpoint classes to `hidden md:inline-flex
  lg:hidden` so it only appears where the drawer actually exists.

  Key Files Modified:
    src/App.tsx (NEW)
    src/app/AppShell.tsx, AppHeader.tsx, BottomTabBar.tsx,
      RequireConnection.tsx, RedirectIfConnected.tsx, PlaceholderPane.tsx
      (NEW)
    src/features/channels/ChannelsPage.tsx (NEW)
    src/features/voice/VoiceMiniBar.tsx (NEW, empty slot)
    src/stores/uiStore.ts (NEW) — settingsOpen / channelDrawerOpen

  Verification:
    Manually driven with Playwright against a throwaway local mock
    socket.io server (not committed — see P1.6's entry) at 375px, 820px,
    and 1440px viewports: no horizontal scroll at any width, the header
    bug above was caught and fixed via a real screenshot, and the fixed
    version was re-verified with fresh screenshots at all three widths.
    npm run typecheck / lint / test / build all pass.

  Next Step: P1.5 — connection store & Socket.io service.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.5: Connection store & Socket.io service

  Problem: No typed transport layer existed to talk to the reson8 server.

  Solution: `socketService` wraps a single `socket.io-client` instance
  typed as `Socket<ServerToClientEvents, ClientToServerEvents>` (from the
  P1.2 vendored types) — deliberately a thin wrapper, not a reimplementation:
  `.on`/`.emit` on the underlying socket are already fully typed, so this
  only adds connection lifecycle (this app never holds more than one
  server connection, per master PRD §7 non-goal 5) and promise wrappers
  for the ack-based events this phase actually uses (USER_JOIN_SERVER,
  USER_JOIN_CHANNEL, PING_LATENCY) rather than a generic emit-with-ack
  utility for the whole protocol up front. `connectionStore` (Zustand)
  holds status/serverId/nickname/instanceId/latencyMs/clockOffsetMs/error.
  `connectionService.connectToServer()` distinguishes a transport-level
  failure (`connect_error` -> "Can't reach that server.") from an
  application-level join rejection (ack.success === false -> the server's
  own error message, e.g. wrong password) so the surfaced message is
  accurate either way. Reconnection relies on Socket.io's built-in
  reconnect (not a custom loop): `socket.on("disconnect", ...)` ignores
  the "io client disconnect" reason (an intentional leaveServer() call)
  and otherwise flips status to "reconnecting"; `socket.io.on("reconnect",
  ...)` flips it back to "connected".

  Key Files Modified:
    src/services/socketService.ts, connectionService.ts (NEW)
    src/stores/connectionStore.ts (NEW)

  Verification:
    Exercised live against the throwaway mock server (P1.6's entry):
    successful join, wrong-password rejection, and the ping loop all
    behaved as designed. Reconnect-after-server-restart was implemented
    per the design above but not independently re-verified this session
    (ran out of session time after the rest of Phase 1 landed) — flagged
    as the first thing to manually confirm before closing out the phase.
    npm run typecheck / lint / test pass.

  Next Step: P1.6 — connect screen.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.6: Connect screen

  Problem: No UI existed for a user to enter server URL / nickname /
  password and join a server.

  Solution: React Hook Form + Zod (serverUrl required, nickname 2-32
  chars, password optional, rememberMe boolean). `normalizeServerUrl()`
  infers `ws://` for localhost/127.0.0.1 and `wss://` otherwise when the
  user omits a scheme (matches browsers' own mixed-content stance since
  this PWA will itself be served over https). Remember Me persists to
  `localStorage` under the desktop client's exact key names
  (`reson8-remember-me`, `reson8-server-url`, `reson8-nickname`,
  `reson8-server-password`) per CLAUDE.md's stated convention. Inline
  error banner reads `connectionStore.error`, which connectionService
  populates with the transport-vs-application-error distinction described
  in P1.5. Password field has a show/hide toggle (Eye/EyeOff icons).

  Manual E2E verification setup (for future sessions): no real `reson8`
  server was available, so I wrote a throwaway ~80-line mock Socket.io
  server (not committed anywhere in this repo) implementing just
  USER_JOIN_SERVER (with a hardcoded "secret" password check),
  CHANNEL_TREE_UPDATE (a small fixture tree — a text category with a text
  channel and a voice channel with two occupants, plus a top-level NSFW
  text channel), PING_LATENCY, and USER_JOIN_CHANNEL. Drove it with
  Playwright (playwright-core, since @playwright/test's own runner isn't
  suited to ad-hoc scripts) to click through connect -> channel tree ->
  settings -> about tab, screenshotting at 375/820/1440px. Zero
  console/page errors throughout. This confirmed the full P1.5/P1.6/P1.8/
  P1.9/P1.10 vertical slice actually works end-to-end, not just in
  isolation per-component.

  Accessibility fix found via this session's axe-core pass (see P1.3):
  the connect screen's root was a bare `<div>`, tripping axe's "region"
  rule ("some page content is not contained by landmarks"). Changed the
  root element to `<main>`.

  Key Files Modified:
    src/features/connection/ConnectScreen.tsx (NEW)
    src/lib/serverUrl.ts, rememberMe.ts (NEW)

  Verification:
    Live manual flow against the mock server described above (success,
    wrong-password, and validation-error paths all confirmed). npm run
    test passes (3 ConnectScreen tests: renders, nickname-too-short
    validation, submits normalized values — connectToServer mocked).
    npm run test:e2e (axe-core) passes after the <main> fix.

  Next Step: P1.7 — instance identity.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.7: Instance identity

  Problem: Reson8 has no login — the client needs a stable, self-generated
  identity per browser profile.

  Solution: `getOrCreateInstanceId()` reads `localStorage['reson8-instance-
  id']`, generating via `crypto.randomUUID()` and persisting on first call
  if absent. No dev/packaged distinction (there's no such concept on the
  web). "Copy Instance ID" button wired into the About tab (P1.10) using
  `navigator.clipboard.writeText`, with a 2s check-icon confirmation state.

  Key Files Modified:
    src/lib/instanceId.ts (NEW)
    src/features/settings/AboutTab.tsx (NEW) — Copy Instance ID button

  Verification:
    npm run test passes (2 instanceId tests: creates+persists, returns
    same id on repeat calls). Confirmed live in the mock-server flow (see
    P1.6) — About tab showed a real, stable-looking UUID.

  Next Step: P1.8 — channel tree read-only render.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.8: Channel tree (read-only render)

  Problem: No UI existed to render the hierarchical channel tree or live
  presence.

  Solution: `channelTreeStore` holds the nested `IChannelTreeNode[]` tree
  from CHANNEL_TREE_UPDATE plus a flat `nodesById` Map rebuilt on every
  update for O(1) lookup (needed repeatedly in later phases, per the PRD).
  Presence is folded into this same store rather than a separate
  presenceStore (the PRD explicitly leaves this as a judgment call, and
  the master PRD's own "State/Data Additions" section for this phase only
  lists channelTreeStore, not a separate presence store) —
  `updatePresence(channelId, occupants)` patches the matching node's
  `.occupants` in place and shallow-clones the root array so subscribers
  re-render. `<ChannelTreePanel>` recursively renders categories
  (collapsible, chevron toggle), text channels (unread dot placeholder off
  `hasUnread`, NSFW badge), and voice channels (occupant count + expanded
  occupant list with mute/deafen/away glyphs). Rows have no real
  navigation yet (Phase 2/4 add it) — tapping a leaf channel shows a
  "coming soon" toast rather than being inert, per the PRD's stated
  developer's-call language.

  Key Files Modified:
    src/stores/channelTreeStore.ts (NEW)
    src/features/channels/ChannelTreePanel.tsx (NEW)

  Verification:
    Confirmed live against the mock server's fixture tree (see P1.6): the
    category/text/voice/NSFW/occupant rendering all matched the fixture
    exactly across mobile, tablet-drawer, and desktop-sidebar
    presentations (screenshotted). npm run lint / typecheck pass.

  Next Step: P1.9 — status/latency display.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.9: Status/latency display

  Problem: No latency/connection-quality indicator existed.

  Solution: `connectionService` runs a 3s PING_LATENCY loop (fires
  immediately on connect, then every 3000ms) computing RTT and an
  NTP-style clock offset (`offset ≈ serverTime - (localSendTime + rtt /
  2)`), both stored on connectionStore for Phase 2's session timers to
  reuse without re-deriving the mechanism. `<LatencyIndicator>` shows a
  color-coded dot (green <100ms, amber <250ms, red otherwise; gray/"—" if
  disconnected or not yet measured) plus the numeric ms value, with
  role="status" aria-live="polite" so screen readers announce changes.

  Key Files Modified:
    src/features/connection/LatencyIndicator.tsx (NEW)
    src/services/connectionService.ts — ping loop (see P1.5 entry)

  Verification:
    Confirmed live against the mock server: indicator showed real,
    plausible single-digit-to-low-double-digit ms values and updated on
    the 3s cadence. npm run typecheck / lint pass.

  Next Step: P1.10 — settings shell.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.10: Settings shell

  Problem: No always-available Settings surface existed.

  Solution: `<SettingsSheet>` — a hand-rolled, not Radix-based, modal
  (full-screen on mobile, centered panel at lg:) with the 7 tabs the
  desktop client has (Roles, Emojis, Server, Voice & Shortcuts,
  Application, Audio, About), all but About/Application rendering a
  "coming soon" placeholder (Server's placeholder is connection-aware:
  "Connect to a server to manage this" when disconnected). Basic a11y by
  hand rather than via Radix Dialog: `role="dialog" aria-modal="true"`,
  focus moved to the panel on open, Escape-to-close, backdrop-click-to-
  close. About tab is fully functional (P1.7's Copy Instance ID + a
  build-time-injected version string — see below). Application tab has
  the "Mute sound alerts" toggle wired to settingsStore.

  Known gap, flagged rather than silently left: @radix-ui/react-dialog and
  @radix-ui/react-tabs were installed (P1.3) specifically so this
  component could use them, but it still uses the hand-rolled version
  above. Manual focus-trapping is real but less robust than Radix's
  (e.g. it doesn't loop Tab at the panel boundaries). Worth swapping to
  the Radix primitives in a follow-up pass rather than treating this as
  done-done — the accessibility upgrade Radix gives "for free" per master
  PRD §5.5 isn't actually realized here yet.

  Build-time version injection: `vite.config.ts` reads package.json's
  version and exposes it as the `__APP_VERSION__` global (declared in
  src/vite-env.d.ts), consumed by AboutTab.

  Key Files Modified:
    src/features/settings/SettingsSheet.tsx, AboutTab.tsx,
      ApplicationTab.tsx (NEW)
    vite.config.ts — __APP_VERSION__ define
    src/vite-env.d.ts (NEW)

  Verification:
    Confirmed live against the mock server: opened via the header gear
    icon, About tab showed version 0.1.0 and a real instance ID, Escape
    closed it. npm run typecheck / lint pass.

  Next Step: P1.11 — sound alert infrastructure.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.11: Sound alert infrastructure

  Problem: No sound-alert asset pipeline or playback helper existed, and
  every later phase's sound cues depend on getting the autoplay-unlock
  pattern right once, here.

  Solution: Copied the desktop client's actual .mp3 assets from
  `../reson8/apps/client/assets/sound-alerts/` into `public/sounds/`
  (reusing exact filenames, per CLAUDE.md's asset-key convention) rather
  than authoring placeholders. `soundAlert.init()` (called once from
  main.tsx) attaches one-time pointerdown/keydown listeners that flip an
  `unlocked` flag; `soundAlert.play(name, category)` is a no-op until that
  flag is set, and otherwise lazily constructs (and caches) a plain
  `new Audio(...)` per sound name — no AudioContext needed for one-shot
  local-file playback (the "detached new Audio() produces no sound"
  pitfall in CLAUDE.md is specific to consumed mediasoup MediaStream
  tracks, not this case). Respects settingsStore's muteAlerts flag and
  per-category volume (alert vs nudge). `connected`/`disconnected` are the
  only two events wired to sounds this phase, per scope.

  localStorage Keys: reson8-mute-alerts, reson8-alert-volume,
  reson8-nudge-volume (settingsStore) — values only this phase; slider UI
  ships in Phase 3's Audio tab per the PRD.

  Key Files Modified:
    src/lib/soundAlert.ts (NEW)
    src/stores/settingsStore.ts (NEW)
    public/sounds/*.mp3 (NEW, 19 files copied from ../reson8)
    src/main.tsx — soundAlert.init() call
    src/services/connectionService.ts — connected/disconnected play() calls

  Verification:
    npm run typecheck / lint pass. Did not verify actual audio output in
    this session (headless browser automation, no audio device) — the
    unlock-gating logic and event wiring were checked by reading the code
    path, not by hearing sound. Flagged for a real-browser manual check
    before treating this item as fully closed.

  Next Step: P1.12 — toast system.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.12: Toast system

  Problem: No toast/notification surface existed for the rest of the app
  to report errors and status changes through.

  Solution: `toastStore` (Zustand) + a generic `toast({title, description?,
  variant?})` helper (auto-dismiss after 5s). `<Toaster>` renders the
  queue with `role="status"` (default variant) or `role="alert"`
  (destructive variant) and `aria-live="polite"` on the container — same
  gap as P1.10's SettingsSheet: @radix-ui/react-toast is installed but
  not used yet; this is a plain-div implementation with the same public
  API a future Radix-backed version would have, so callers don't need to
  change when that swap happens.

  Key Files Modified:
    src/stores/toastStore.ts (NEW)
    src/components/ui/toaster.tsx (NEW)
    src/App.tsx — <Toaster /> mounted at the root

  Verification:
    Exercised indirectly via ChannelTreePanel's "coming soon" toast and
    connectionService's ERROR-event toast during the mock-server manual
    flow (P1.6) — not independently screenshotted, but rendered without
    console errors. npm run lint / typecheck pass.

  Next Step: P1.13 — PWA baseline.

--- Entry: 14/08/2026 ---

Feature/Fix: Phase 1 — P1.13: PWA baseline

  Problem: No manifest or service worker existed; the build needed to
  produce an installable artifact from the start rather than bolting PWA
  on in Phase 7.

  Solution: `vite-plugin-pwa` in `generateSW` mode (Phase 7 switches to
  `injectManifest` once the custom update-prompt logic needs it — flagged
  in vite.config.ts so Phase 7 isn't surprised). Manifest: name "Reson8",
  theme_color/background_color matching the dark token palette (P1.3),
  display: "standalone", icons at 192/512/512-maskable.

  Known gap: the three manifest icon files are all the *same* 512x512
  PNG (copied from ../reson8/logo_512x512.png) with different `sizes`
  attributes declared — no image-resize tooling (no ImageMagick, no
  Pillow, no sharp) was available in this sandbox to actually produce a
  192x192 or a properly-padded maskable variant. This is very likely
  fine for Lighthouse's installability check (it validates the manifest
  fields and that an icon URL resolves, not pixel-exact dimension
  matching) but is real follow-up work — either install an image tool in
  a normal dev environment and regenerate proper sizes, or source real
  192/512/maskable assets from design.

  Key Files Modified:
    vite.config.ts — VitePWA plugin config
    public/icons/icon-192.png, icon-512.png, icon-maskable-512.png (NEW,
      all currently identical 512x512 source images — see gap above)

  Verification:
    npm run build succeeds and generates dist/manifest.webmanifest,
    dist/sw.js, dist/workbox-*.js (13 precache entries, ~760 KiB). Did not
    run an actual Lighthouse audit this session (no Chrome DevTools/CI
    Lighthouse runner available) — the acceptance criterion "Lighthouse
    PWA installability check passes" is unverified, only "the build
    produces the artifacts Lighthouse would check" is confirmed.

  Next Step: Phase 1 acceptance criteria verification, then /bump-version.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 1 acceptance criteria verification

  Problem: Needed to actually check the phase's acceptance criteria against
  a running instance rather than trusting the per-item entries above in
  isolation, since several were built and manually spot-checked one at a
  time rather than as a full end-to-end pass.

  Solution: Ran every acceptance criterion against the built app driven by
  Playwright, live-connected to the same throwaway mock server described
  in the P1.6 entry. Found and fixed three real bugs this pass caught:

  1. axe-core on the connected app shell (not just the connect screen)
     found 2 violations neither the P1.3/P1.6 per-item checks had
     exercised: (a) the "18+" NSFW badge's destructive-on-destructive-
     foreground text only hit 3.17:1 contrast (WCAG needs 4.5:1 for small
     text) — darkened --destructive from oklch(0.65 ...) to oklch(0.5 ...)
     in src/index.css; (b) no page-level <h1> existed once you leave the
     connect screen — changed AppHeader's "Reson8" title span to an <h1>.
     Re-ran axe on both screens after the fix: 0 violations.

  2. Live resize of a single running session from 375px to 1280px and back
     confirmed no horizontal scroll at either width, the channel tree
     stays rendered across the breakpoint change, and the socket
     connection / ping loop are untouched by the layout change (latency
     indicator kept ticking) — matching the acceptance criterion exactly.

  3. Killing the mock server mid-session and restarting it (kill -9,
     ~13s outage, then a fresh process on the same port) surfaced a real
     gap in P1.5's reconnect handling: on Socket.io's built-in
     `reconnect` event, the client resumed the ping loop and flipped
     status back to "connected" without ever re-sending USER_JOIN_SERVER.
     Since the server treats each socket connection as a fresh session,
     a bare transport-level reconnect isn't sufficient — the client would
     show "connected" while never receiving another CHANNEL_TREE_UPDATE
     or PRESENCE_UPDATE for that session. Fixed in
     src/services/connectionService.ts: `attachLifecycleListeners` now
     takes the original join params and re-runs the join handshake inside
     the `reconnect` handler before declaring the connection recovered
     (and surfaces an error + disconnects if the rejoin itself is
     rejected, e.g. the user was banned during the outage).

  Remaining gaps, honestly flagged rather than silently claimed as done:
    - Never tested against a real `reson8` server — only the throwaway
      mock described in P1.6. The mock only implements the handful of
      events this phase touches (USER_JOIN_SERVER, USER_JOIN_CHANNEL,
      PING_LATENCY, CHANNEL_TREE_UPDATE emit); it does not validate
      nickname-length/format rules, real password hashing, or any
      server-side edge cases a real deployment would exercise.
    - No Lighthouse PWA-installability audit was run (no Chrome DevTools
      / Lighthouse CI available in this sandbox) — only confirmed the
      build produces the manifest + service worker + precache artifacts
      Lighthouse would check.
    - Sound alert playback (P1.11) was verified by code-reading the
      unlock-gating logic, not by hearing actual audio output (headless
      sandbox, no audio device).
    - mobile-safari (webkit) Playwright project crashes on page.goto in
      this proot/ARM64 sandbox specifically — chromium and mobile-chrome
      (Pixel 7 emulation) both pass everything. Not treated as a product
      bug; flagged for Phase 7's real-device pass.
    - Manifest icons are a single 512x512 source image relabeled at three
      sizes (no image-resize tool available in this sandbox) — see P1.13.

  Key Files Modified:
    src/index.css — --destructive token contrast fix
    src/app/AppHeader.tsx — title span -> <h1>
    src/services/connectionService.ts — reconnect now re-joins

  Verification:
    npm run typecheck / lint / test all pass (11/11 unit tests). npm run
    test:e2e passes on chromium + mobile-chrome (4/4: connect-screen
    no-horizontal-scroll, accessibility x2). axe-core: 0 violations on
    both the connect screen and the connected app shell. Manual
    Playwright-driven pass covering: connect (success + validation-error
    paths), channel tree render with occupants/NSFW/unread-dot fixtures,
    latency indicator ticking, Settings sheet + About tab content, tablet
    drawer open/close, 375<->1280px live resize, and the mock-server
    kill/restart reconnect scenario above — zero console/page errors
    throughout.

  Next Step: Phase 1 complete — run /bump-version, then begin Phase 2
  (mediasoup voice core).

=== Version bumped to 1.0.0 (15/08/2026) — classification: MAJOR ===

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.1: voiceService architecture

  Problem: Phase 2 needs a client-side mediasoup engine to drive the SFU
  voice handshake, but it must stay framework-agnostic so it's independently
  testable and so the desktop client's Phase 11 reconnection logic ports
  with only browser-API substitutions, not a redesign (Phase 2 PRD P2.1).

  Solution: Added `src/services/voiceService.ts` — a plain TS class
  (`VoiceService`) holding a mediasoup-client `Device`, send/recv
  `Transport`s, the active `Producer`, a `Map` of `Consumer`s keyed by
  consumer id, and a `Map` of DOM `<audio>` elements keyed by consumer id.
  Takes a narrow `VoiceSignaling` interface in its constructor (six
  promise-returning methods matching the wire protocol's voice events 1:1)
  instead of importing `socketService` directly, so it can be unit-tested
  with a fake signaling object. Exposes `onConnectionLost`/`onError`
  callback properties (event-emitter-style, not React-coupled) that
  `voiceConnectionService` wires up.

  Key Files Modified:
    src/services/voiceService.ts (NEW)

  Verification:
    npm run typecheck / lint pass. Covered indirectly by every later P2.x
    item's verification below; the mute/deafen state machine specifically
    got direct unit tests (see the P2.5 entry).

  Next Step: P2.2 — wire the six-step handshake through voiceService.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.2: Voice handshake (6-step)

  Problem: Joining a voice channel requires driving mediasoup-client through
  a specific six-step sequence against the server's ack-based Socket.io
  events: GET_ROUTER_CAPABILITIES -> CREATE_WEBRTC_TRANSPORT (x2, send +
  recv) -> CONNECT_TRANSPORT -> PRODUCE -> CONSUME -> RESUME_CONSUMER.

  Solution: `socketService.ts` gained six promise-wrapped methods
  (`getRouterCapabilities`, `createWebRtcTransport`, `connectTransport`,
  `produce`, `consume`, `resumeConsumer`) satisfying `VoiceSignaling`.
  `VoiceService.joinVoiceChannel()` drives steps 1-4 in order
  (getRouterCapabilities -> device.load() -> createSendTransport ->
  createRecvTransport -> startProducing, which itself does getUserMedia +
  produce). `consumeProducer()` (called per remote producer, not part of
  the initial join) drives CONSUME + RESUME_CONSUMER for steps 5-6.
  Producers that arrive before the recv transport is ready are queued in
  `pendingProducers` and flushed once it's up.

  A generic type helper (`AckPayload<Ev>`) in socketService.ts extracts the
  ack callback's resolved type from the vendored `ClientToServerEvents`
  signatures for CREATE_WEBRTC_TRANSPORT/CONSUME; a naive variadic-tuple
  version resolved to `never` for these two-argument event signatures, so
  it uses an explicit two-branch conditional type with a `payload: never`
  contravariant-parameter trick instead.

  Key Files Modified:
    src/services/socketService.ts
    src/services/voiceService.ts

  Verification:
    npm run typecheck / lint pass. End-to-end handshake verified against a
    stub mock server extended specifically for this (see the final
    acceptance entry for full detail and honest caveats — no real
    reson8/mediasoup server was available in this sandbox).

  Next Step: P2.3 — getUserMedia & permission UX.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.3: getUserMedia & permission UX

  Problem: Unlike Electron's auto-grant, every browser shows a real,
  per-origin mic permission prompt, and a denial needs a clear,
  non-dead-end recovery path (master PRD's platform-limitations table).

  Solution: `VoiceService.startProducing()` calls `getUserMedia` only in
  direct response to the join gesture (never speculatively), with
  echoCancellation/noiseSuppression/autoGainControl constraints and an
  optional exact deviceId. `voiceConnectionService.joinVoiceChannel()`
  catches the rejection and classifies it: `err instanceof DOMException &&
  (err.name === "NotAllowedError" || err.name === "PermissionDeniedError")`
  sets `permissionDenied: true` on the returned result, distinct from any
  other join failure. `src/features/voice/MicPermissionDeniedPanel.tsx`
  (NEW) renders a denial-specific recovery UI: a platform-detected
  guidance string (iOS Safari / Android / Firefox / generic, via user-agent
  sniffing) plus a "Try again" button that re-invokes the join. Wired into
  both `VoicePanel` (persistent lg: pane / mobile sheet) and surfaced via
  `useJoinVoice` (NEW hook) so `ChannelTreePanel`'s voice row tap also
  shows it.

  Key Files Modified:
    src/services/voiceConnectionService.ts
    src/features/voice/MicPermissionDeniedPanel.tsx (NEW)
    src/hooks/useJoinVoice.ts (NEW)
    src/features/voice/VoicePanel.tsx
    src/features/channels/ChannelTreePanel.tsx

  Verification:
    npm run typecheck / lint pass. The `permissionDenied` classification
    branch itself is covered by a deterministic unit test in
    src/services/voiceConnectionService.test.ts (feeds a real
    `NotAllowedError`/`PermissionDeniedError` DOMException through the
    catch block and asserts the flag) rather than a live E2E denial click —
    reproducing the exact DOMException name a real browser's permission
    prompt produces was unreliable in this sandbox's headless Chromium
    (see the final acceptance entry for detail). Live E2E did confirm the
    general failure path (any getUserMedia rejection) surfaces a graceful
    "Couldn't join voice" toast with no crash and a recoverable idle state.

  Next Step: P2.4 — audio playback.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.4: Audio playback (DOM-attached audio elements)

  Problem: A detached `new Audio()` produces no sound for a consumed
  mediasoup track — bit the desktop client once — and mobile Safari's
  autoplay/media-element policies are stricter still, so this matters more
  for a web client, not less (CLAUDE.md conventions section).

  Solution: `VoiceService.consumeProducer()` creates the audio element via
  `document.createElement("audio")`, sets `srcObject` to a `MediaStream`
  wrapping the consumer's track, sets `autoplay = true`, and appends it to
  `document.body` before calling `.play()` (wrapped in a no-op `.catch()`
  since autoplay can still be rejected post-gesture on some browsers, not
  independently user-actionable beyond retrying the join). Tracked in the
  `audioElements` Map keyed by consumer id so `removeConsumer()` and
  `cleanup()` can pause/detach/`remove()` them, and so `toggleDeafen()` can
  mute/unmute every live element in one pass.

  Key Files Modified:
    src/services/voiceService.ts

  Verification:
    npm run typecheck / lint pass. Confirmed via code review + the DOM
    presence of appended `<audio>` elements during the E2E connected-state
    pass (see final acceptance entry) that the element-creation/attachment
    path executes; actual audible playback could not be verified — headless
    sandbox, no real remote producer/audio track since the stub mock server
    has no real SFU.

  Next Step: P2.5 — mute/deafen state machine.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.5: Mute/Deafen accumulation state machine

  Problem: The desktop client's mute/deafen interaction has a real, subtle
  discrepancy between its own Phase 10 PRD text (which says Mute should be
  a no-op while deafened) and its actual shipped `renderer.ts` behavior
  (which auto-undeafens first, then applies a toggle on top). Porting "the
  exact state machine" means porting the shipped code, not the PRD's stated
  intent — confirmed via a targeted extraction pass against the desktop
  repo before implementing this item.

  Solution: Split the state machine across two layers matching the
  existing service/orchestration split:
  - `VoiceService.toggleDeafen()` (mode-agnostic): deafening auto-mutes the
    producer and sets `_deafenAutoMuted = true` only if the producer wasn't
    already paused; undeafening restores exactly that remembered state
    (resumes only if `_deafenAutoMuted`) and always unmutes every attached
    audio element. `toggleMute()`/`setMuted()` are no-ops while deafened.
  - `voiceConnectionService.toggleMute()` (mode-aware): if currently
    deafened, auto-undeafens via `voiceService.toggleDeafen()` first, then
    applies a toggle on top of the *resolved* mute state from that
    call — not a no-op, matching the desktop's real behavior rather than
    its PRD text (documented in both files' doc comments so the
    discrepancy doesn't get "fixed" back to the PRD's stated intent later).

  Key Files Modified:
    src/services/voiceService.ts
    src/services/voiceConnectionService.ts

  Verification:
    npm run typecheck / lint pass. Added
    src/services/voiceService.test.ts (9 unit tests) — injects a fake
    producer object via the class's private field to drive the matrix
    directly without a real mediasoup handshake: toggleMute pause/resume in
    both directions, no-op while deafened (both toggleMute and setMuted),
    deafen-while-unmuted auto-mutes, deafen-while-already-muted leaves the
    producer alone AND undeafen does not incorrectly auto-unmute it,
    undeafen-after-auto-mute correctly restores unmuted, audio-element
    mute/unmute on deafen toggle, and toggleMute/setMuted never throw
    before a producer exists. Added 5 more scenario tests in
    src/services/voiceConnectionService.test.ts specifically for the
    click-Mute-while-deafened auto-undeafen-then-toggle branch. All pass.
    Also confirmed live end-to-end against the connected stub-server state
    (see final acceptance entry): Mute -> Unmute, Deafen -> Undeafen, and
    Deafen-then-Undeafen-restores-still-muted all reproduced through real
    UI clicks against a real mediasoup-client Producer object.

  Next Step: P2.6 — PTT vs Voice Activation.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.6: Push-to-Talk vs Voice Activation

  Problem: Need both a default "always transmitting unless muted" mode and
  an opt-in PTT mode, with PTT's press/release semantics kept independent
  of the persistent Mute button's "locked" state — conflating the two would
  make the Mute button unpredictable while a PTT key is being held.

  Solution: `voiceStore.pttMode: boolean` persisted to
  `localStorage['reson8-ptt-mode']`. In PTT mode, the store's `isMuted`
  flag is repurposed as a "locked" flag toggled only by the Mute
  button/shortcut — NOT the live transmit state.
  `voiceConnectionService.pttPress()`/`pttRelease()` call
  `voiceService.setMuted(false/true)` directly, bypassing the lock flag
  entirely, and are no-ops if PTT mode is off, not connected, locked, or
  deafened. `applyInitialMuteState()` sets the resting state to
  transmit-blocked on join in PTT mode while keeping the store's `isMuted`
  (lock) false so the UI shows the mic as available. `PttButton.tsx` (NEW)
  is a press-and-hold on-screen button (pointer down/up + pointerleave/
  cancel handling) — the primary PTT affordance on mobile, since there is
  no OS-level global hotkey on the web (master PRD platform-limitations
  table); a focused-tab keydown/keyup listener via
  `voiceShortcutService.ts` (NEW) covers desktop keyboard PTT.

  Key Files Modified:
    src/stores/voiceStore.ts
    src/services/voiceConnectionService.ts
    src/features/voice/PttButton.tsx (NEW)
    src/lib/keyboardShortcut.ts (NEW)
    src/stores/shortcutStore.ts (NEW)
    src/services/voiceShortcutService.ts (NEW)
    src/main.tsx — attachVoiceShortcutListeners() wired in

  Verification:
    npm run typecheck / lint pass. voiceConnectionService.test.ts covers:
    Mute-click toggles the lock flag without calling voiceService.toggleMute
    in PTT mode; unlocking does not itself resume transmission; pttPress
    transmits only when PTT mode + connected + unlocked + not deafened;
    pttPress/pttRelease no-ops for each of those gating conditions. Live
    E2E confirmed the Settings tab's PTT/Voice-Activation toggle buttons
    render and the active-variant styling flips on click.

  Next Step: P2.7 — device selection.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.7: Audio device selection

  Problem: Users need to pick a specific mic/speaker, with graceful
  handling of `setSinkId`'s Chromium-only support and the browser privacy
  behavior where device *labels* stay blank until a permission grant has
  occurred at least once for the origin.

  Solution: `src/lib/audioDevices.ts` (NEW):
  `enumerateAudioDevices()` (falls back to "Microphone N"/"Speaker N"
  placeholders when labels are empty), `supportsOutputDeviceSelection()`
  (feature-detects `"setSinkId" in HTMLMediaElement.prototype`), and
  `applyOutputDeviceToLiveAudioElements()` (calls `setSinkId` on every
  currently-mounted `<audio>` element, silently ignored where unsupported).
  `voiceStore` gained `selectedInputDeviceId`/`selectedOutputDeviceId`
  (persisted to `reson8-audio-input`/`reson8-audio-output`) plus staged
  variants + `applyStagedDevices()` so device changes in Settings require
  an explicit Save rather than applying live mid-selection. Input device
  changes apply "next time you join voice" (copy in the UI); output device
  changes apply live via `applyOutputDeviceToLiveAudioElements()`.

  Key Files Modified:
    src/lib/audioDevices.ts (NEW)
    src/stores/voiceStore.ts
    src/features/settings/VoiceShortcutsTab.tsx (NEW, device section)

  Edge Cases Handled:
    Speaker selection unsupported (Safari/Firefox) -> explanatory text
    instead of a non-functional dropdown, rather than hiding the section.

  Verification:
    npm run typecheck / lint pass. Live E2E confirmed the microphone
    device `<select>` renders in Settings -> Voice & Shortcuts. Output
    `setSinkId` behavior could not be exercised live (headless sandbox, no
    real audio output devices to enumerate/select between).

  Next Step: P2.8 — Active Speaker Indicator.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.8: Active Speaker Indicator

  Problem: Occupants need a visual cue for who's currently talking, driven
  by the server's ACTIVE_SPEAKERS broadcast.

  Solution: `voiceStore.activeSpeakerUserIds: Set<string>`, updated by
  `voiceConnectionService.handleActiveSpeakers` (ignores payloads for a
  channel other than the one currently joined). `VoicePanel`'s occupant
  list and `ChannelTreePanel`'s voice-row occupant chips both render a ring
  halo (`ring-2 ring-success`) around a speaking occupant's avatar.

  Key Files Modified:
    src/stores/voiceStore.ts
    src/services/voiceConnectionService.ts
    src/features/voice/VoicePanel.tsx
    src/features/channels/ChannelTreePanel.tsx

  Verification:
    npm run typecheck / lint pass. Not exercised live — the stub mock
    server never emits ACTIVE_SPEAKERS (no real SFU-side audio-level
    detection to drive it); verified by code review that the halo class is
    conditional on store membership and the handler correctly filters by
    channel.

  Next Step: P2.9 — session timer.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.9: Voice session timer

  Problem: Need a flicker-free MM:SS session timer sourced from the
  server's `sessionStartedAt` (not client-side join time, so it stays
  correct across a reconnect) that never shows a negative duration.

  Solution: `src/lib/sessionTimer.ts` (NEW) computes the formatted duration
  directly from `sessionStartedAt` and clamps at "00:00" if the computed
  delta is negative (clock skew). `src/hooks/useSessionTimer.ts` (NEW)
  forces a re-render once a second via `useReducer` rather than staging the
  formatted string in `useState` — computing the display value directly in
  the render body avoids a one-tick-stale flicker that a staged-state
  approach would show. `voiceConnectionService.handlePresenceUpdateForVoice`
  updates `sessionStartedAt` from PRESENCE_UPDATE when it changes.

  Key Files Modified:
    src/lib/sessionTimer.ts (NEW)
    src/hooks/useSessionTimer.ts (NEW)
    src/services/voiceConnectionService.ts

  Verification:
    npm run typecheck / lint pass. Live E2E confirmed the timer renders as
    MM:SS in the connected voice panel. Multi-minute duration and the
    negative-delta clamp were verified by code review, not by waiting out a
    real multi-minute session in this pass.

  Next Step: P2.10 — reconnection resilience.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.10: Reconnection resilience

  Problem: Voice needs to survive (a) a WebRTC transport genuinely losing
  its connection, (b) the underlying Socket.io connection dropping and
  reconnecting (which the server treats as a fresh session, so voice must
  fully rejoin, not just resume), (c) a server-side mediasoup worker crash
  (VOICE_SESSION_LOST), and (d) mobile browsers silently losing a
  backgrounded tab's WebRTC connection without ever firing a clean
  disconnect — a real risk flagged in the PRD as mobile-web-specific,
  beyond anything the desktop Electron client ever faced.

  Solution: Four independent triggers converge on the same
  `attemptVoiceRejoin()` (retries the full 6-step handshake up to 3 times,
  1.5s apart, giving the server time to finish cleaning up the old session
  before a retry can race it):
  1. `VoiceService`'s transport `connectionstatechange` handler: "failed"
     reports immediately; "disconnected" gets a 4000ms grace timer first
     (ICE frequently self-recovers, e.g. a brief NAT rebind) before
     reporting — reported at most once per join via `connectionLossReported`.
  2. `connectionService`'s Socket.io `reconnect` handler now calls
     `voiceConnectionService.rejoinVoiceIfNeeded()` after the server-level
     rejoin succeeds.
  3. `handleVoiceSessionLost` (VOICE_SESSION_LOST listener) triggers a
     rejoin when the payload's channelId matches `lastVoiceChannelId`.
  4. A new `document.visibilitychange` listener in `connectionService`
     calls `voiceConnectionService.rejoinVoiceIfConnectionLost()` on the
     visible transition, which only actually rejoins if the store's status
     isn't already "connected" in the last-known channel — covers the
     silent-backgrounded-tab-loss case with no extra signal from the
     browser. UI copy in VoiceShortcutsTab explicitly sets expectations as
     best-effort ("Voice may disconnect if you leave the app — reopen to
     reconnect"), not a seamless-continuity promise.
  After `REJOIN_MAX_ATTEMPTS` exhausts, the store fully resets and a
  destructive toast tells the user to rejoin manually rather than leaving
  the UI stuck in "reconnecting" forever.

  Key Files Modified:
    src/services/voiceService.ts
    src/services/voiceConnectionService.ts
    src/services/connectionService.ts

  Verification:
    npm run typecheck / lint pass. The retry-exhaustion and
    trigger-gating logic were verified by code review; a live
    kill/restart-the-signaling-server rejoin pass (the kind of test done
    for Phase 1's P1.5 reconnect fix) was not repeated here for voice
    specifically — doing so meaningfully requires a real mediasoup server
    on the other end (per this phase's own PRD: "Requires a reachable
    reson8 dev/staging server with mediasoup correctly configured... for
    any real voice testing"), which isn't available in this sandbox. The
    4000ms ICE-disconnected grace period and the mobile-backgrounding
    visibilitychange trigger are both explicitly called out in the PRD
    itself as needing real-device verification, deferred to Phase 7.

  Next Step: P2.11 — voice mini-bar & panel.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.11: Voice mini-bar & panel

  Problem: Voice controls need to stay reachable without ever being buried
  behind chat navigation, across three distinct layout compositions
  (mobile, tablet, desktop) — per the master PRD's "one component set,
  three compositions" principle, not three separate implementations.

  Solution: `VoicePanel.tsx` (rewritten from the Phase 1 stub) is the single
  full-featured component (occupant list with speaking/muted/deafened/away
  indicators, mute/deafen/leave controls, PTT button when in PTT mode,
  MicPermissionDeniedPanel when applicable) rendered in two places by
  `AppShell.tsx`: persistently in a `lg:` third column, and inside a
  mobile/tablet full-screen sheet gated by `uiStore.voicePanelOpen` (NEW
  state). `VoiceMiniBar.tsx` (rewritten) is a slim always-visible bar
  (channel name, status/timer, mute/deafen/leave icon buttons) pinned above
  the mobile tab bar (`lg:hidden`) that expands the sheet on tap, modeled
  on a "now playing" mini-player so it's never one navigation-tap away from
  invisible.

  Key Files Modified:
    src/features/voice/VoicePanel.tsx
    src/features/voice/VoiceMiniBar.tsx
    src/app/AppShell.tsx
    src/stores/uiStore.ts

  Verification:
    npm run typecheck / lint pass. Live E2E confirmed: the persistent
    desktop (1440px) panel renders with working mute/deafen/leave controls
    against a real connected session; the mobile (375px) mini-bar renders
    while voice is active. Tablet-breakpoint sheet-open/close interaction
    specifically for voice (as opposed to the general drawer pattern
    already verified in Phase 1) was not separately re-driven this pass.

  Next Step: P2.12 — sound cues.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 — P2.12: Voice sound cues

  Problem: Voice actions need the desktop client's established sound-alert
  vocabulary (asset-key convention already defined in CLAUDE.md), including
  the rule that presence-diff events fire exactly one sound per event, not
  one per user in the diff.

  Solution: `soundAlert.play(...)` calls added at each transition point:
  `joining-channel`/`leaving-channel` on join/leave,
  `mic_muted`/`mic_activated` and `sound_muted`/`sound_resumed` on
  mute/deafen toggles (deafen toggle sound intentionally reuses the
  sound_muted/sound_resumed pair, matching the desktop convention that
  deafen's audio cue is about the user's own audio being cut, same as a
  self-mute), `connected` on a successful rejoin. `previousOccupantIds` (a
  module-level `Set<string>` in voiceConnectionService, diffed every
  PRESENCE_UPDATE via `handlePresenceUpdateForVoice`) drives
  `user_joined_channel`/`user_disconnected_from_channel` — computed as
  "did any id newly appear / disappear across the whole diff", called at
  most once per event regardless of how many users joined/left
  simultaneously.

  Key Files Modified:
    src/services/voiceConnectionService.ts

  Verification:
    npm run typecheck / lint pass. Verified by code review that each call
    site fires the correct asset key and that the presence-diff logic
    computes hadJoins/hadLeaves as event-level booleans, not per-user.
    Actual audio output was not verified (headless sandbox, no audio
    device) — same caveat as Phase 1's P1.11 sound-alert entry.

  Next Step: Phase 2 acceptance verification.

--- Entry: 15/08/2026 ---

Feature/Fix: Phase 2 acceptance verification

  Problem: Same rationale as Phase 1's acceptance pass — individual P2.x
  items were built and spot-checked mostly in isolation; needed one pass
  against a running app checking the phase's actual Acceptance Criteria,
  and honest reporting on what could/couldn't be verified given this
  sandbox has no reachable reson8/mediasoup server (a limitation the phase
  PRD itself calls out as a hard requirement for full voice testing).

  Solution: Extended the same throwaway mock Socket.io server used for
  Phase 1 (mock-server/server.mjs, scratchpad-only, never committed) with
  stub handlers for the full voice handshake — GET_ROUTER_CAPABILITIES
  (returns one real-shaped Opus codec entry so mediasoup-client's
  Device.load() actually succeeds), CREATE_WEBRTC_TRANSPORT (returns
  plausibly-shaped but fake iceParameters/iceCandidates/dtlsParameters,
  including a correctly-lengthed 32-byte sha-256 DTLS fingerprint — an
  under-length fingerprint was tried first and correctly produced a real
  `setRemoteDescription`/"Failed to apply remote fingerprint" WebRTC error,
  which usefully proved the graceful-failure/toast path works before being
  fixed to let the happy path complete too), CONNECT_TRANSPORT, and PRODUCE
  (acks with a fake producerId). This is enough for a real
  mediasoup-client Device/Transport/Producer to be constructed
  client-side and for the join to reach a genuine "connected" state with a
  real (locally-instantiated, non-networked) Producer object — sufficient
  to drive real UI interactions against real state, though no actual SFU
  audio ever flows (no CONSUME from a second real participant was set up).

  Added two new Vitest unit-test files for the pieces most valuable to
  verify deterministically rather than via a flaky live browser pass:
  - src/services/voiceService.test.ts (9 tests) — the mute/deafen state
    machine in isolation, injecting a fake producer via the class's
    private field.
  - src/services/voiceConnectionService.test.ts (13 tests) — the
    mode-aware layer on top (mute-while-deafened auto-undeafen-then-toggle,
    PTT lock-flag semantics, pttPress/pttRelease gating, and the
    permissionDenied DOMException-name classification), using vi.mock for
    VoiceService/socketService/soundAlert/toastStore.

  Ran every acceptance criterion reachable in this sandbox against the
  built app via a Playwright script (permissions: ["microphone"] +
  --use-fake-device-for-media-stream / --use-fake-ui-for-media-stream
  Chromium launch args for the granted-permission path):
  - Full join handshake completes and reaches "connected" (session timer
    visible) against the stub server — real Device/Transport/Producer
    objects, no crash.
  - Mute -> Unmute toggles correctly against a real Producer.
  - Deafen -> Undeafen toggles correctly; Deafen-then-Undeafen while
    already muted before deafening correctly restores "still muted" (the
    exact discrepancy-from-desktop-PRD behavior called out in the P2.5
    entry), reproduced through real button clicks this time, not just
    injected unit-test state.
  - A getUserMedia failure (no fake device + no granted permission,
    producing NotFoundError/NotSupportedError in this headless
    environment rather than the exact NotAllowedError a real browser
    denial click would produce) surfaces a graceful "Couldn't join voice"
    toast and returns the panel to a clean "Not in a voice channel" idle
    state — no crash, no stuck spinner. The permissionDenied=true
    classification branch specifically is instead verified deterministically
    by the unit test described above, since reproducing the exact
    NotAllowedError DOMException name via headless Chromium's permission
    model was unreliable in this sandbox (three different attempts
    produced three different DOMException names depending on flag
    combination — a headless-environment quirk, not a client bug).
  - Settings -> Voice & Shortcuts tab: PTT/Voice-Activation toggle buttons
    render and the active state flips on click; all 4 shortcut recorder
    rows (PTT/Mute/Deafen/Disconnect) render "Set"/"Clear" controls;
    microphone device `<select>` renders.
  - Mobile (375px) mini-bar renders while voice is active; desktop
    (1440px) persistent third-column panel renders with working controls.
  17/17 checks in this pass, zero console/page errors across every
  scenario.

  Remaining gaps against the phase's stated Acceptance Criteria, honestly
  flagged rather than silently claimed as done:
    - "Two browser tabs... hear each other with acceptable latency on a
      local/LAN test server" — NOT verified. Requires a real mediasoup SFU
      actually routing RTP between two producers/consumers; the stub mock
      server has no real media plane at all. This is the PRD's own
      explicitly-flagged hard dependency ("Requires a reachable reson8
      dev/staging server with mediasoup correctly configured... coordinate
      server availability before starting this phase's implementation") —
      no such server was available in this sandbox for the whole phase.
    - "Backgrounding the mobile browser tab for 30s... triggers a
      visibility-driven reconnection... verified via real device testing,
      not just emulation" — NOT verified via a real device; the trigger
      wiring was verified by code review only (see P2.10 entry). The PRD
      itself defers real confirmation to Phase 7's device-testing pass.
    - Mediasoup-worker-crash recovery (VOICE_SESSION_LOST) — handler wiring
      verified by code review only; no real server to actually crash a
      worker against.
    - Actual audible playback of remote/local audio was not verified — no
      audio device in this headless sandbox (same caveat as Phase 1's
      sound-alert entry).
    - mobile-safari (webkit) Playwright project still crashes on
      page.goto in this proot/ARM64 sandbox — same pre-existing,
      already-documented sandbox limitation from Phase 1 (see that
      phase's acceptance entry), unrelated to any Phase 2 code; chromium
      and mobile-chrome e2e specs (unrelated to voice — connect-screen +
      accessibility) both still pass.

  Key Files Modified:
    (none beyond the P2.1-P2.12 entries above; this entry covers
    verification tooling only)
    src/services/voiceService.test.ts (NEW)
    src/services/voiceConnectionService.test.ts (NEW)

  Verification:
    npm run typecheck / lint / test all pass (33/33 unit tests across 5
    files, up from 11 at the end of Phase 1). npm run test:e2e: 4/6 pass
    (chromium + mobile-chrome; mobile-safari fails on the pre-existing
    sandbox limitation noted above) — unchanged from Phase 1, confirming
    no regression to the Phase 1 specs. Manual Playwright-driven voice
    pass: 17/17 checks passing, detailed above.

  Next Step: Phase 2 complete — run /bump-version (MAJOR, per the PRD:
  this phase ships new capabilities throughout), then begin Phase 3.

=== Version bumped to 2.0.0 (15/08/2026) — classification: MAJOR ===
